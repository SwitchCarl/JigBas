# JigBas 阶段性记录 — 2026-08-04

> 本文件作为会话间上下文交接文档，下次会话开始时可读取本文件恢复进度。
> 总规划见 `E:\Desktop\Jigbas\Context.txt`（4 周方案）。

## 协作规则（用户要求，务必遵守）

1. **出现任何环境问题时：停下测试并询问用户**，不要自行反复尝试。
2. **出现代码更改后：停下并请求用户远程推送代码**（git push 需用户确认）。

## 项目现状

- 第 1 周基线流水线已完成并验证通过：`core.py`（Wespeaker 声纹比对 → FunASR Paraformer ASR）、`ui.py`、`JigBas.py`、`build_dataset.py`（Lhotse 混音三元组构建）。
- 端到端验证结果（ECAPA 示例音频）：同说话人 sim=0.61 正确接受并转写；异说话人 sim=0.009 正确拒识。
- `Records/` 目前只有本文件，尚无基线指标。

## 本次会话已完成

1. **`core.py` 增加 GPU 设备支持**（代码更改，待推送）：
   - `ModelHub(device=None)`：默认自动选 `cuda:0`（可用时）否则 `cpu`
   - wespeaker 加载后调用 `set_device(device)`
   - FunASR `AutoModel(..., device=device, disable_update=True)`
2. **新建 `evaluate.py`**（代码更改，待推送）：基线评估脚本
   - 对 manifest 批量推理，每条样本只跑一次流水线（ASR 与阈值无关）
   - 阈值扫描（默认 0.30:0.75:0.05）输出 CER / 拒识率 RR / FAR / FRR / 综合分 / 耗时 RTF
   - 结果写入 `Records/eval_<时间戳>.json`，`--detail` 可出逐样本明细
   - 用法：`python evaluate.py --manifest Dataset/dev_manifest.jsonl --data-root Dataset [--limit 50]`
   - 冒烟测试清单已备好在 `%TEMP%\jigbas_eval_smoke\manifest.jsonl`（临时目录，重启后需重建）

## 进行中的任务（下次会话继续）

### 任务 A：换装 GPU 版 torch（cu128）— ✅ 已完成

- torch 2.8.0+cu128 / torchaudio 2.8.0+cu128 已从阿里云镜像下载（存于 `wheels/`，勿提交 git）并安装成功。
- 验证通过：cuda_available=True，RTX 5070 Ti（sm_120），matmul 正常。
- **发现并修复 wespeaker 库 bug**：其 fbank 前端不随 `set_device` 迁移，GPU 下设备不匹配崩溃。方案：wespeaker 固定 CPU（`ModelHub(wespeaker_device="cpu")`，CPU 提取仅 ~0.4s/条），FunASR 用 `cuda:0`。
- `evaluate.py` 冒烟测试通过（FunASR GPU + wespeaker CPU）：阈值 0.3~0.6 时 CER 0%、拒识率 100%，RTF 0.10；阈值 0.7 时正样本被误拒（FRR 100%），阈值扫描逻辑符合预期。结果样例存于 `JigBas/Records/eval_20260804_125921*.json`。

### 任务 B：构建数据集 + 跑基线评估（依赖手动下载的语料）

1. 语料就绪后运行：
   ```bash
   python build_dataset.py --clean-dir <AISHELL wav\train> --noise-dir <MUSAN noise> \
       --output Dataset --num-train 2000 --num-dev 200 --trim
   python build_dataset.py --stats --output Dataset
   ```
2. 跑基线：`python evaluate.py --manifest Dataset/dev_manifest.jsonl --data-root Dataset --detail`
3. 将最优阈值与 CER/RR/RTF 记录到 `Records/`，作为第 1 周基线。

### 后续（第 2 周，规划中）

- 说话人条件 ASR：Paraformer 编码器注入说话人嵌入（FiLM/拼接），联合 ASR 损失 + 说话人存在性损失，拒识样本强制空白输出。
- 需要 GPU 训练（任务 A 是前置）。

## 需要手动下载的内容（用户操作）

| 内容 | 用途 | 来源 | 大小 |
|---|---|---|---|
| AISHELL-1 | 干净中文语料（按说话人分目录，构建三元组主料） | OpenSLR 33: https://www.openslr.org/33/ （或网盘镜像） | ~15GB |
| MUSAN | 噪声库（用其中 noise 目录，含空调等非人声噪声） | OpenSLR 17: https://www.openslr.org/17/ | ~11GB |
| aidatatang_200zh（可选） | 扩充说话人数量 | OpenSLR 62: https://www.openslr.org/62/ | ~17GB |
| RIR 冲激响应库（可选） | 真实混响增强（不装则用脚本内置合成 RIR） | OpenSLR 26/28 | ~1-5GB |
| torch cu128 wheels（仅当自动下载失败时手动下） | GPU 版 torch | https://mirrors.aliyun.com/pytorch-wheels/cu128/torch-2.8.0%2Bcu128-cp39-cp39-win_amd64.whl 及同名 torchaudio | ~2.7GB |

下载后请放到固定目录（建议 `E:\corpus\`），并告知实际路径。

## 待办提醒

- [ ] 代码推送：`core.py`、`evaluate.py`、`Records/` 已改动，等用户确认 git push
- [x] ~~torch wheel 安装 + CUDA 验证（任务 A）~~ ✅ 完成
- [x] ~~`evaluate.py` 冒烟测试~~ ✅ 通过（wespeaker 固定 CPU 的修复已生效）
- [ ] 语料下载（用户手动，见上表）
- [ ] 构建数据集 + 基线评估（任务 B）
- [ ] `wheels/` 加入 .gitignore
